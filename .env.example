# MinIO / S3-compatible storage
MINIO_ROOT_USER=chessia
MINIO_ROOT_PASSWORD=change_me_to_a_real_secret
MINIO_ENDPOINT_URL=http://localhost:9000
MINIO_RAW_BUCKET=chessia-raw
MINIO_PROCESSED_BUCKET=chessia-processed

# Ollama (inference server for finetuned models)
OLLAMA_URL=http://localhost:11434

# Hugging Face token, only needed if the base model turns out to be gated
HF_TOKEN=
